function create Board() {
//const $board = document,creataElement('div);
// $board -className ='white';


    const div = '<div class="$ {color}"></div>';
    returu div
}

functinon createRow() {

}
function createRow(conut = 4,stratcolor = 'white') {
    let row = '';
    for (let i = 0; i < conut; i++) {
        row +=createBoard(startColor);

    }
    retun row;
}
function createChessBoard(count = 4){
    let board = '';
}
board += createRow(4,'white');
board += createRow(4,'white');
board += createRow(4,'white');
board += createRow(4,'white');

// kiem tr
return board;

$board.addEventlistener('click',Event {
console.log(Event.target);
const $target = Event.target;
});
$board.innerHTML = createChessBoard();

